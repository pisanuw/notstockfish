# Briefing

- Purpose: Teaching tool chess web app with progressive AI engines (v0-v4) for AI/algorithms courses
- Current scope: FastAPI backend with chess engines, React+TypeScript frontend, deployed via Netlify
- Key decisions: Project display name is "stockreptile" (deliberate rename); GitHub repo slug remains "notstockfish" — needs manual rename in GitHub Settings to align. History purge of committed .venv not yet done (requires git filter-repo + force push confirmation).
- Non-goals: Multiplayer (deferred), magic-link/Google auth (deferred)
